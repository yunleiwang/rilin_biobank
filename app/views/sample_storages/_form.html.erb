<%= form_for(@sample_storage  , :html=>{:class=>'form-horizontal',:role=>'form'}) do |f| %>
  <% if @sample_storage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample_storage.errors.count, "error") %> prohibited this sample_storage from being saved:</h2>

      <ul>
      <% @sample_storage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<input type="hidden" name="id" value="<%= params[:sample_storage_id]%>">
    <div class="widget wgreen">
      <div class="widget-head">
        <div class="pull-left"></div>
        <div class="widget-icons pull-right">
          <a href="#" class="wminimize"><i class="fa fa-chevron-up"></i></a>

        </div>

        <div class="clearfix"></div>
      </div>

      <div class="widget-content">
        <div class="padd">
        </div>

        <div class="form-group">
          <div  class="col-sm-2">
            <%= label_tag :sample_seq, '样本信息',:class=>'col-lg-2 control-label' %>
          </div>
          <div class="col-sm-4">
            <%= text_field_tag :sample_seq,nil,:class=>'form-control', :placeholder=>'样本信息' %>
          </div>
          <div  class="col-sm-2">
          </div>
          <div class="col-sm-4">
          </div>
        </div>


      </div>
    </div>
    <button class="btn btn-lg btn-primary" type="submit">提交</button>
  <!--<div class="actions">
    <%#= f.submit %>
  </div>-->
<% end %>
